apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/name: makeademo
  app.kubernetes.io/part-of: makeademo

resources:
  - manifests/deployment.yaml
  - manifests/service.yaml
  - manifests/serviceaccount.yaml
